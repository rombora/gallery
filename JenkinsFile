pipeline {
    agent any
    stages {
        stage ("clone repository") {
            steps {
                git "https://github.com/rombora/gallery"
            }
        }
        
        stage ("install dependencies") {
            steps {
                sh "npm install"
            }
        }
    }
}
